# Photo Publisher

`photo_publisher.py` is the source for Cha Lin's local desktop publishing tool.
It resizes selected photos, strips metadata, updates `public/photos/photos.json`,
and publishes only the photo files through the repository's existing Git login.

The generated Windows executable is intentionally ignored by Git.
